# DEPRECATED — do not use.
#
# Deploy.sa now uses a PER-SITE WAF model: the build agent starts a dedicated
# ModSecurity container (deploysa-waf-<slug>) for each project that enables WAF,
# with a higher-priority Traefik router. There is no shared ModSecurity proxy.
#
# See WAF.md. This file is kept only to avoid breaking references and must NOT
# be `docker compose up`-ed (its wildcard router would capture all app traffic).
